apiVersion: v1
kind: Pod
metadata:
  name: kiada-hostname
spec:
  hostname: custom-hostname
  subdomain: custom-subdomain
  containers:
  - name: kiada
    image: quay.io/maciek01220/kiada:0.4
    ports:
    - name: http
      containerPort: 8080


